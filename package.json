{
  "name": "integrator-pro",
  "version": "1.0.0",
  "description": "Business Management System for Custom Integration Industry",
  "main": "server/index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build && cd ../server && npm run build && cd ../api && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install && cd ../api && npm install",
    "setup-db": "cd server && npm run setup-db"
  },
  "keywords": ["business", "inventory", "invoicing", "pos", "integration"],
  "author": "Integrator Pro",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
} 